Building Instructions:

1) cd "your path to the source code"
2) repo sync -j16 
3) . build/envsetup.sh
4) lunch cm_loganreltexx-userdebug
5) time brunch loganreltexx -j# , where "#" is the number of CPU cores+1, for example on a Dual Core it will be -j3
